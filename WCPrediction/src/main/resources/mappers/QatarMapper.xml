<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.ofemperor.WCPrediction.dao.QatarDAO">
    <select id="getPlayerData" resultType="hashMap">
        select
            *
        from
            players;
    </select>
</mapper>